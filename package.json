{
  "name": "genezio-marketer-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML documentation site generator for Genezio",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist",
    "dev": "npm run build && npx -y http-server dist -p 3000",
    "preview": "npx -y http-server dist -p 3000"
  }
}
